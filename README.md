# push-button-counter
A push button counter is an embedded system circuit that detects every press of a tactile switch as a digital input signal. The microcontroller tracks these inputs using a software variable, incrementing the count by one each time the button is pressed.
